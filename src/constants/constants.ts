export enum ButtonClassType {
  'PRIMARY' = 'primary',
  'SECONDARY' = 'secondary'
}

export enum ButtonType {
  'BUTTON' = 'button',
  'SUBMIT' = 'submit'
}

export enum ButtonSize {
  'SMALL' = 'small',
  'NORMAL' = 'normal',
  'BIG' = 'big'
}

export enum PageRoutes {
  'HOME_PAGE' = '/',
  'ENCODING_PAGE' = '/encodings'
}

export const API_KEY = 'API_KEY';
